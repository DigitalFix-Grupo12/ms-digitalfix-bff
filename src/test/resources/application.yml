spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://example.com/fake-issuer
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop

digitalfix:
  security:
    expected-audience: test-audience
    allowed-origins: http://localhost:4200
